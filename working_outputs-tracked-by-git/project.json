{
  "name": "included-outputs",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "test-project/src",
  "projectType": "library",
  "targets": {
    "a": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "echo a",
          "echo A $(date) >> cachestuff/included/output-a.log"
        ],
        "parallel": false
      },
      "inputs": [
        "{workspaceRoot}/cachestuff/input-a"
      ],
      "outputs": [
        "{workspaceRoot}/cachestuff/included/output-a.log"
      ]
    },
    "b": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "echo b",
          "echo B $(date) >> cachestuff/included/output-b.log"
        ],
        "parallel": false
      },
      "inputs": [
        "{workspaceRoot}/cachestuff/included/output-a.log"
      ],
      "outputs": [
        "{workspaceRoot}/cachestuff/included/output-b.log"
      ],
      "dependsOn": [
        "a"
      ]
    },
    "c": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "echo c",
          "echo C $(date) >> cachestuff/included/output-c.log"
        ],
        "parallel": false
      },
      "inputs": [
        "{workspaceRoot}/cachestuff/included/output-b.log"
      ],
      "outputs": [
        "{workspaceRoot}/cachestuff/included/output-c.log"
      ],
      "dependsOn": [
        "b"
      ]
    }
  }
}